#AI Phising URL Detector

A machine learning project that detects phisings URLs using basic features engineering and 
classification models.

##tech stack

-python 3.14
-scikit-learn,xgboost
-streamlit

##setup

```bash
python -m venv venv
venv\scripts\activate
pip install -r requirements.txt
```# future-dev
my dev env
